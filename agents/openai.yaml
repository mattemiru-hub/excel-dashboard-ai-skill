interface:
  display_name: "Excel Dashboard AI"
  short_description: "Build refreshable Excel BI with mandatory intake"
  default_prompt: "Use $excel-dashboard-ai-skill to inspect my Excel data first, ask the required intake questions, propose the right dashboard blueprint if needed, and then build a refreshable Power Query + Power Pivot + DAX + PivotChart executive dashboard."
